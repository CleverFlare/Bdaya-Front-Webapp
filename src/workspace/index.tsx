import { Typography } from "@mui/joy";
import { FC } from "react";
interface IWorkspaceProps {}

export const Workspace: FC<IWorkspaceProps> = (props) => {
  return <Typography>Workspace</Typography>;
};
